[Unit]
Description=OpenSSH %i Server Key Generation
ConditionFileNotEmpty=|!/var/lib/private/ssh/ssh_host_%i_key

[Service]
Type=oneshot
ExecStart=/usr/lib/sshd-keygen %i

[Install]
WantedBy=sshd-keygen.target
